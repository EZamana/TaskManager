import { GetCapitalFirstLetterPipe } from './get-capital-first-letter.pipe';

describe('GetCapitalFirstLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCapitalFirstLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
